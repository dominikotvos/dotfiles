# Tray icon: use the themed SVG from the icon theme instead of rasterising the
# layout name ("en") into a small pixmap, which is inherently low-res and
# blurred when the bar scales it.
PreferTextIcon=False
ShowLayoutNameInIcon=False
