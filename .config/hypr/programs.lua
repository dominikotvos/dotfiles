-- Programs used by both hyprland.lua and keys.lua.
-- Single source so the two files can never drift apart.

return {
    terminal    = "kitty",
    fileManager = "dolphin",
    menu        = "/home/sleuth/.config/rofi/launchers/type-4/launcher.sh",
}
